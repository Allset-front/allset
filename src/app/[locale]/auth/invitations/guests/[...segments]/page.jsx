"use client"

import { useParams } from "next/navigation";

export default function Guests() {
  // const { id } = useParams();

  // Here you would fetch guests for this invitation
  // e.g., fetch(`/api/invitations/${id}/guests`)

  return (
    <div>
      {/* <h1>Guests for Invitation {id}</h1> */}
    </div>
  );
}
